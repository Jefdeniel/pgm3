// file contains some constants that will be used elsewhere in the application
import path from "path";

export const SOURCE_PATH = path.resolve("src");

// export const PORT = process.env.PORT || 3000;
